import { AxisContentDirective } from './axis-content.directive';

describe('AxisContentDirective', () => {
  it('should create an instance', () => {
    const directive = new AxisContentDirective();
    expect(directive).toBeTruthy();
  });
});
